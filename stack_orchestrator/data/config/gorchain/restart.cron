# Gorchain node restart cron schedules
# Format: <minute> <hour> <day-of-month> <month> <day-of-week> <command>
#
# Schedules:
# - RPC: every 6 hours at :30 (staggered to avoid simultaneous restarts)

# Restart RPC node every 6 hours at :30
30 */6 * * * /scripts/restart-node.sh role=rpc
