stack: gorchain-rpc
deploy-to: k8s-kind
network:
  ports:
    gorchain-rpc:
      - '8899:8899'   # RPC port
      - '8900:8900'   # WebSocket port
volumes: {}
configmaps:
  gorchain-config: ./config
